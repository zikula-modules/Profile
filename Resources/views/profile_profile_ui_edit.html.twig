{# { pageAddAsset('js', zasset('@ZikulaProfileModule:js/Profile.UI.Edit.js')) } #}

{% for fieldSet in fieldSets %}
    <fieldset class="{{ fieldSet|e('html_attr') }}">
        <legend>{{ fieldSet }}</legend>
        {% for itemLabel, item in dudItems if item.prop_fieldset == fieldSet %}
            {{ dudItemModify(item=item, uid=userid, error=duderrors[itemlabel]|default('')) }}
        {% endfor %}
    </fieldset>
{% endfor %}
