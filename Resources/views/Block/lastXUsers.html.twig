{% if users %}
<ul class="list-group">
    {% for user in users %}
        <li class="list-group-item">{{ user.uname|profilelinkbyuname() }} ({{ user.user_regdate|localizeddate('medium', 'none') }})</li>
    {% endfor %}
</ul>
{% endif %}
